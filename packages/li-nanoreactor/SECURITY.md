# Security

Report vulnerabilities privately to the li-langverse security contact. This
package ships pure-Li numerics only; it links no C runtime of its own and
performs no allocation (fixed 8-atom stack arrays).
