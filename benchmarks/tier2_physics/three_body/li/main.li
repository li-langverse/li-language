# Li three_body — driver over shared C reference kernel.

extern def li_three_body_kernel()

def main() raises IO -> int
  requires true
  ensures result == 0
  decreases 0
=
  li_three_body_kernel()
  return 0
