@cpu
@Dparallel(disjoint=disjoint_row)@cpu
@paracpu
@parallel(disjoint=disjoint_row)
proc f-) -> int
  requires true
  ensures true
 =
  
@lilelds(joint=disjoint_row)
proc f true
  decreases 0
=
  
@pdr
proc f(( -> strint
  requires tparallel(disjoiparallel(disjoint=disjae