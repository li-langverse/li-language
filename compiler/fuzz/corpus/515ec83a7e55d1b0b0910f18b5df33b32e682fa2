@cpu
@paf() -> int
  requires true
  ensures true
  t
  requires true
  ensures true
  decreases# Parr). Nightly CI merges new inputs here via `sc.
o,p-l 0
e@cpu
@parallel Nightly CI merges new inputs here via `sc(di=
  returs