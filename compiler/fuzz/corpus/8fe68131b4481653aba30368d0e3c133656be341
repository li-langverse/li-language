@cpule(lldisjoint=disjoint_row)
proc f]) raises
  requires tcpu
@parale(lldisjoint=disjoint_row)
proc f]) -> int
  requires true
  ensures @cpu
@
@parallel(disjorow)
proc f(parallel(disj) -> int
  requires tint=disjo 0
