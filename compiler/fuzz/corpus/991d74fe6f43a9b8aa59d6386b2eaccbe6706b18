@cpu
@parallel(disjoint=disjoint_row)
proc f() -> int
  requires true
  ensures true
  decreases 0
=
  ret ensures true
  t
  requires true
  ensures true
  decreases# Parr). Nightly CI merges new inputs here via `sc.
o,p-l 0
e@cpu
@parallel Nightly CIurn 0
