# Security: a function requiring false can never be called.
# Calling it from reachable code must fail at check time (E0304).
def unreachable() -> int
  requires false
  ensures result == 0
=
  return 0

def main() -> int
  requires true
  ensures result == 0
=
  return unreachable()
