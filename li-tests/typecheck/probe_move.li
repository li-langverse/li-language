def twice(x: int, y: int) -> int
  requires true
  ensures result == 0
  decreases 0
=
  var a: int = x + y
  return a

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  var p: int = 3
  var q: int = 4
  var r: int = twice(p, q)
  var s: int = p + q
  return r + s
